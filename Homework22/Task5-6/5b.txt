List li;
li.push_front(1);
auto cur = li.begin();
double num;
while (cur != li.end())
{
    cout << "Input a number: ";
    cin >> num;
    if (num > 5)
        li.push_back(num);
    ++cur;
}
 
та сама ситуація що і в 5a тільки тут ми зразу отримаємо end актуальну, хоча воно і так завжди nullptr, пройде стільки елементів скільки користувач задасть за умовою. 